# Bello Landing Page

Static landing page implemented from a Figma design.

## Structure

- `index.html` - page markup
- `styles.css` - page styles and hover interactions
- `assets/` - images and SVG files used by the page

## Run

Open `index.html` in a browser. No build step or JavaScript dependencies are required.
